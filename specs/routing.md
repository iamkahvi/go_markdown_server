# routing

A.
- /write/<doc name>
- /read/<doc name>

B.
- /<doc name>/write
- /doc name>/read

in the UI there should be a status dot indicator where red is disconnected. Yellow is in the process of connecting green is connected and synced.

so that way, even if you're disconnected, you can still write, but it should be understood that you aren't syncing to the server
